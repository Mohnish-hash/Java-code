// Print The array 
class demo
{
    public static void main(String[] args)
    {
      // Datatype arrayname[]={};
      // Datatype arrayname[]=new Datatype[Size];
      // Datatype []arrayname=new Datatypr[Size];

      int i,arr[]={10,20,30,40,50};
      for(i=0; i < arr.length;i++)
      {
        System.out.println(arr[i]);
      }  

    }
}
